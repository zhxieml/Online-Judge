#include <iostream>
using namespace std;

int main() 
{	
    int n, opNum, tmp;
    char op;

    for (int i = 0; i < n; i++)
    {
        cin >> op;

        if (op == '+')
        {
            cin >> opNum;

            for (int i = 0; i < opNum; i++)
            
        }
    }

    return 0;
}