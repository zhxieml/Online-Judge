#include <iostream>
#include "linkQueue.h"
using namespace std;

template <class Type>
void tree<Type>::createTree(Type flag)
{
    linkQueue<node *> que;
    node *tmp;
    Type x, son;
}
